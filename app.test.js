const { colours, btn, colour, getRandomColour, setRandomBackground } = require('./app');

  test('chooses a random colour from array', () => {
    setRandomBackground();
    // expect(document.body.style.backgroundColor).toBe(colours[backgroundColour]);
  });
